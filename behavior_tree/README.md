# Behavior Tree

Simple scheme of a behavior tree in python

---

## Language Note

For the purpose of convenience and better team alignment, the language of the commands and nodes used within the behavior tree is Polish.

## Installation

To run the behavior tree scripts, you will need the `py_trees` library. 

You can easily install it using `pip` in your terminal or command prompt:

```bash
pip install py_trees